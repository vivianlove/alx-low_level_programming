#include <stdio.h>

/**
 * main - prints the name of the program
 * @rgc: number of arguments passed to the function
 * @argv: argument vectir of pointers to string
 * Return: always 0
int main(int argc _attribute_((unused)), char *argv[])
{
	   printf("%\n", argvargv[0]);
	return (0);
}
