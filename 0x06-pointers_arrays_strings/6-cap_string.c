#include "main.h"

/**
 * Cap_string - Capitalizes all words of a string.
 * @str:  The string to be capitalized.
 *
 * Return: A pointer to the changed string
 */
char *cap_string(char *)
{
	int index = 0;

	while (str[index]
        {
	      while(!(str[index] >= 'a' && str
		     index++;
	       if(str[index - 1] == ' ' ||
	       if(str[index - 1] == '\t' ||
	       if(str[index - 1] == '\n' ||
	       if(str[index - 1] == '.' ||
	       if(str[index - 1] == ';' ||
	       if(str[index - 1] == ',' ||
	       if(str[index - 1] == '|' ||
	       if(str[index - 1] == '?' ||
	       if(str[index - 1] == '"' ||
	       if(str[index - 1] == '(' ||
	       if(str[index - 1] == ')' ||
	       if(str[index - 1] == '{' ||
	       if(str[index - 1] == '}' ||
	          index == 0)
                      Str[index] -= 32;
               index++;		 
       }
      return (str);
}     
